# Mobile Frame UI

A clean React template with a mobile and tablet device simulation wrapper.

## Setup

```bash
yarn install
yarn dev
```

## Configuration

Duplicate `.env.example` to `.env` and set the appropriate environment variables.
